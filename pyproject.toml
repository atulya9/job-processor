[project]
name = "job-processor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "atulya9", email = "tibrewalatulya@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
job-processor = "job_processor:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
